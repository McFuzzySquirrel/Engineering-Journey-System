---
session_id: ejs-session-2026-03-13-01
author: mcfuzzysquirrel
date: 2026-03-13
repo: Engineering-Journey-System
branch: copilot/research-journey-records-efficiency
agents_involved:
  - GitHub Copilot (Coding Agent)
decision_detected: true
adr_links:
  - "0015"
tags:
  - ejs
  - research
  - simplification
  - micro-instructions
  - implementation
refs:
  - ejs-docs/research/ejs-simplification-findings.md
  - .github/copilot-instructions.md
  - .github/skills/ejs-session-init/SKILL.md
  - .github/skills/ejs-sub-agent-capture/SKILL.md
  - scripts/bootstrap-ejs.sh
  - scripts/bootstrap-ejs.ps1
  - README.md
  - ejs-docs/session-lifecycle-patterns.md
  - ejs-docs/adr/0015-micro-instruction-simplification.md
  - ejs-docs/journey/_templates/journey-template.md
---

# Problem / Intent

Research whether the EJS instruction surface can be significantly simplified while preserving core principles (journey recording, ADR creation, agent attribution, sub-agent capture). The hypothesis: could a small, portable instruction block sent to every agent and sub-agent replace the current multi-layered system?

**Session continuation (2026-03-13):** Implement the findings from `ejs-simplification-findings.md` — specifically the 5-phase micro-instruction simplification plan recommended in Section 7.

# Interaction Summary (Required)

- Human: Requested research into whether EJS can be simplified. Suggested the idea of a small instruction sent to every agent and sub-agent to update a journey and create ADRs from templates. No code changes — findings and recommendations only.
  - Agent [GitHub Copilot]: Explored the full repository structure to understand the current system architecture, measured instruction surface area (366 lines across copilot-instructions.md + 3 skills; ~940 words always-on; ~3072 words total including templates), and analyzed each component for essential vs. overhead content.
  - Outcome: Comprehensive understanding of current complexity. Research document created with findings and recommendations.

- Human: (implicit) Core principles that must be preserved: journey records, ADRs, agent attribution, sub-agent capture, agent influence tracking.
  - Agent [GitHub Copilot]: Identified which principles are served by which components, and which components add complexity without proportional value.
  - Outcome: Mapped each principle to the minimum instruction needed to serve it.

- Human: Asked to add an implementation plan to the research document to kick off later after review.
  - Agent [GitHub Copilot]: Reviewed all files that would be affected by implementation (copilot-instructions.md, 3 skills, bootstrap scripts, README, lifecycle docs). Created a 5-phase implementation plan with specific file changes, acceptance criteria, rollback strategy, and effort estimates.
  - Outcome: Research document Section 7 expanded from 6 bullet points to a comprehensive implementation plan with phases, checklists, and a recommended execution order.

- Human: "lets implement as per the ejs-simplification-findings.md"
  - Agent [GitHub Copilot]: Read the full research document (all 5 phases), read current state of all affected files (copilot-instructions.md, 3 skills, 2 bootstrap scripts, README, lifecycle doc), asked two clarifying questions (marker strategy + phase order).
  - Outcome: Implementation plan confirmed — clean break on marker, phased execution (Phase 1 validate first). Session memory plan created.

- Human: "Start implementation"
  - Agent [GitHub Copilot]: Phase 1 — rewrote `.github/copilot-instructions.md` from 112 lines (~940 words) to 33 lines (197 words). New heading `## EJS Recording Contract`. All 6 core behaviors preserved. Checkpoint rules, DB protocol details, and format examples removed (delegated to wrapup skill and templates). Verified: 54/54 tests pass.
  - Outcome: Phase 1 complete and validated. 79% always-on context reduction achieved.

- Human: (implicit — continuing implementation)
  - Agent [GitHub Copilot]: Phases 2–3 executed in parallel. Phase 2a: removed 9-line redundant DB-first block from `ejs-session-init/SKILL.md`. Phase 2c: added Sub-Agent Instruction Fragment section to `ejs-sub-agent-capture/SKILL.md`. Phase 3a/3b: updated detection marker and output messages in both bootstrap scripts. Phase 3c/3d: updated README (3 locations) and session-lifecycle-patterns.md (table + bullets). Phase 5: created ADR 0015, ran `adr-db.py sync` to index it.
  - Outcome: All 10 implementation todos complete. Bootstrap dry-run confirmed new marker output. 54/54 tests pass.

- Human: "whats next?"
  - Agent [GitHub Copilot]: Identified three remaining actions: (1) update this session journey, (2) update PR #14 description to reflect implementation, (3) Phase 4 validation (already happening implicitly in this session).
  - Outcome: Human confirmed — update the session journey.

- Human: "yes" (update session journey)
  - Agent [GitHub Copilot]: Updated session journey with full implementation record — frontmatter, interaction summary, decisions, learnings, agent influence, machine extracts.
  - Outcome: Journey reflects complete research + implementation session.

# Agent Collaboration Summary

Which agents participated (by name): GitHub Copilot (Claude Sonnet 4.6), explore sub-agent
Key suggestions made (and by which agent):
- Agent [GitHub Copilot]: Proposed a "Micro-Instruction" model — a compact ~25-line portable block that replaces the 112-line copilot-instructions.md, delegating detail to templates rather than embedding it in instructions.
- Agent [GitHub Copilot]: Recommended phased execution with Phase 1 validation before cascading changes — adopted by human.
- Agent [GitHub Copilot]: Recommended clean break on bootstrap detection marker (not backward-compat alias) — adopted by human.
- Agent [explore]: Provided comprehensive repository analysis covering all files, their purposes, and relationships.

Corrections applied by human:
- None. Human set direction; agent executed both research and implementation.

# Sub-Agent Contributions

## Sub-Agent: explore (explore agent)
- **Task delegated:** Full repository exploration — understand directory structure, all files, architecture, and relationships
- **Decisions made:** Chose to organize findings by component type (agent, skills, templates, scripts, docs) for clarity
- **Alternatives considered:** Could have done a shallow scan; chose comprehensive analysis to support thorough research
- **Outcome:** Complete repository map with file-level summaries, enabling the complexity analysis
- **Handoff to other agents:** Findings used by main agent for the simplification research

# Agent Influence (Required)

- Suggestions adopted:
  - The three-option structure for recommendations (Micro-Instruction, Consolidated, Hybrid)
  - Quantitative analysis of instruction surface area
  - Phased execution order (Phase 1 validate, then Phases 2–5)
  - Clean break on bootstrap detection marker (no legacy alias)
  - `ejs-session-wrapup/SKILL.md` requires no changes (already correctly placed)
  - Parallel execution of Phases 2–3 after Phase 1 validated
- Suggestions rejected (and why):
  - Backward-compat alias for old bootstrap marker — human opted for clean break
  - All phases in one pass — human preferred phased validation approach
- Human overrides / corrections:
  - None

# Experiments / Evidence

- Measured current instruction surface: 366 lines (copilot-instructions.md + 3 skills), ~940 words always-on, ~3,072 words total including templates
- Analyzed the journey template: 15 sections, 5 machine extracts — significant overhead for sub-agents that only need to append a few lines
- Compared copilot-instructions.md (what agents must always carry) vs. skills (loaded on demand) vs. templates (reference only)
- Key finding: the always-on instructions duplicate much of what the skills and templates already describe
- **Phase 1 result:** New `copilot-instructions.md` = 33 lines, 197 words (target was ≤30 lines / ≤250 words — met on words, 3 lines over but acceptable)
- **Test suite:** 54/54 tests passed after every phase — no regressions introduced
- **Bootstrap dry-run:** New `--dry-run` output correctly references `EJS Recording Contract` marker
- **DB indexing:** `adr-db.py sync` indexed ADR 0015; `search "micro-instruction"` returns it immediately

# Iteration Log

Research phase: No pivots — linear research session.

Implementation phase:
- Phase 1 validated before proceeding to Phases 2–5 (as recommended by agent, adopted by human)
- No unexpected issues required any pivots — all phases executed as planned
- `ejs-session-wrapup/SKILL.md` determined to need no changes during Phase 2 review (checkpoint rules were already correctly placed there from the original instructions)

# Decisions Made

- Decision: Produce research document with three options (Micro-Instruction, Consolidated, Hybrid) with a recommendation for the Micro-Instruction approach.
  - Reason: Directly addresses the user's hypothesis while providing alternatives for comparison.
  - Impact: Informs future implementation decisions; no code changes in this session.

- Decision: Implement Option A (Micro-Instruction) + sub-agent fragment from Option C.
  - Reason: 79% always-on context reduction; sub-agent fragment closes blind spot; templates carry format detail eliminating redundancy.
  - Alternatives considered: Option B (Consolidated — rejected: increases always-on context), Option C alone (Hybrid — not enough reduction), backward-compat alias for bootstrap marker (rejected: added complexity for minimal benefit).
  - Impact: All future sessions in this repo and any repo bootstrapped from it will use the compact 33-line micro-instruction. Existing bootstrapped repos have a transitional marker mismatch.

- Decision: Phased execution — Phase 1 validate, then Phases 2–5.
  - Reason: Highest-value change (copilot-instructions.md) ships first; early validation surfaces issues before cascading changes.
  - Impact: Implementation completed without any required pivots.

- Decision: Create ADR 0015 to record this simplification.
  - Reason: 4 of 6 ADR rubric criteria met (public contract change, alternatives with trade-offs, long-lived consequences, process change).
  - Impact: Decision is now indexed, searchable, and linked to this session journey.

# Key Learnings

- The current always-on instructions (copilot-instructions.md) consume ~940 words of context in every agent interaction, even for simple tasks
- Much of the instruction content is procedural detail (how to checkpoint, how to format entries) that could be delegated to the template itself
- The journey template already contains inline guidance (section headers with descriptions) — duplicating this in instructions is redundant
- Sub-agents receive no EJS context at all in the current model — they depend entirely on the main agent to capture their work after the fact
- Phased implementation with early validation (Phase 1 + tests before continuing) is a lower-risk pattern than all-at-once execution — zero regressions this session
- The `---` separator in `copilot-instructions.md` is a functional contract (bootstrap scripts `sed` extract from it) — not just formatting; must be preserved in any future rewrites
- Redundancy quantification ("same content in 4 places") is essential to justify simplification to reviewers; vague claims of bloat are not persuasive

# If Repeating This Work

Do this: Start with quantitative measurement of instruction surface area before proposing changes
Do this: Run tests after Phase 1 before proceeding — early validation prevents cascading failures
Do this: Check bootstrap script extraction logic before changing the copilot-instructions.md structure
Avoid this: Proposing changes without understanding how all components interact
Watch out for: The `---` separator dependency between copilot-instructions.md and the bootstrap `sed` command
Watch out for: The difference between "always-on" context cost (copilot-instructions.md) vs. "on-demand" cost (skills loaded when relevant)

# Future Agent Guidance

Prefer: Compact, self-contained instruction blocks that reference templates rather than embedding procedural detail
Prefer: Phased execution with early validation checkpoints for cascading multi-file changes
Avoid: Duplicating guidance across copilot-instructions.md, skills, and templates
Avoid: Removing the `---` separator from copilot-instructions.md (bootstrap scripts depend on it)
When extending EJS instructions: ask "is this already in a template or skill?" before adding to always-on block

## MACHINE EXTRACTS

### INTERACTION_EXTRACT
Session covered both research and implementation. Research phase: Agent explored repository, measured complexity (366 lines always-on instructions, ~940 words, ~3072 words total), and produced research document with three simplification options (Micro-Instruction, Consolidated, Hybrid). Implementation phase: Human asked to implement per research findings. Agent asked two clarifying questions (marker strategy, phase order), planned 5 phases, then executed: (1) rewrote copilot-instructions.md 112→33 lines / ~940→197 words; (2) simplified ejs-session-init skill, added sub-agent fragment to capture skill; (3) updated bootstrap scripts marker + messages, README 3 locations, session-lifecycle-patterns.md; (5) created ADR 0015 and indexed it. All 54 tests passed. Bootstrap dry-run confirmed. Human confirmed session journey update as final step.

### DECISIONS_EXTRACT
- Implement Option A (Micro-Instruction) + sub-agent fragment from Option C — 79% always-on context reduction; closes sub-agent blind spot
- Phased execution: Phase 1 validate first, then Phases 2–5 — reduces risk of cascading failures
- Clean break on bootstrap detection marker (`## EJS Recording Contract`) — simpler than maintaining legacy alias
- Create ADR 0015 — 4/6 rubric criteria met (public contract, alternatives considered, long-lived, process change)

### LEARNING_EXTRACT
- Always-on instructions cost ~940 words per agent interaction — compressible to 197 words with no behavior loss
- Separation of concerns: instructions = behavior, templates = format, skills = procedural detail on-demand
- Sub-agent blind spot is a common multi-agent failure mode; a ~40-word delegation fragment closes it at near-zero cost
- Bootstrap scripts have a structural dependency on `---` separator in copilot-instructions.md — must be preserved
- Phased implementation with early validation prevents cascading failures in multi-file changes

### AGENT_GUIDANCE_EXTRACT
Keep always-on instructions under 250 words. Ask "is this already in a template or skill?" before adding to copilot-instructions.md. When delegating to sub-agents, include the Sub-Agent Instruction Fragment from ejs-sub-agent-capture/SKILL.md. Preserve the `---` separator in copilot-instructions.md — bootstrap scripts depend on it. When implementing multi-file changes: phase the work, validate Phase 1 first.

### SUB_AGENT_EXTRACT
Explore sub-agent (research phase): Full repository analysis covering directory structure, all files, purposes, and relationships. Chose comprehensive analysis over shallow scan. Output used as foundation for the complexity measurement and redundancy map. No handoff to other sub-agents (research session).

### SUB_AGENT_EXTRACT
- explore agent: Full repository analysis — directory structure, file contents, architecture mapping
- Output used as input for simplification analysis
